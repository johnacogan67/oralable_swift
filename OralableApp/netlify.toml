# Use only if Netlify Base directory = `OralableApp`
# Prefer repo-root `netlify.toml` (publish = OralableApp/docs) when Base directory is empty.
[build]
  publish = "docs"
  command = ""

# Headers for better caching and security
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"

[[headers]]
  for = "/images/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000"

# Redirect www to non-www (if using custom domain)
[[redirects]]
  from = "https://www.oralable.com/*"
  to = "https://oralable.com/:splat"
  status = 301
  force = true
